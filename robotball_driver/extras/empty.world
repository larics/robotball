include "definitions.inc"

# load an environment bitmap
floorplan
( 
	name "empty"
	bitmap "empty.bmp"
	size [7 7 1]  # duzina sirina visina
	pose [ 0 0 0 0 ]
)

# throw in robots
sphero( pose [ -2.5 0.0 0 0.000 ] name "sphero_0" color "blue")
